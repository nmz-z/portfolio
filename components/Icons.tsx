import React from "react"
/*import Image from 'next/image'
import styles from './icon.module.scss'
import adobephotoshop from "./home/icons/adobephotoshop.svg"
import ReactTooltip from 'react-tooltip'

type StaticImageData = {
  src: string;
  height: number;
  width: number;
  placeholder?: string;
};

type IconProps = {
  defaulIcon: StaticImageData
  onHoverIcon: StaticImageData
  href?: string
  name: string
  tooltipID?: string
}

export function VIcon(props: IconProps) {

  return (
    <a data-tip={props.tooltipID ? true : false} data-for={props.tooltipID} href={props.href} className={styles.iconContainer}>
      <div className={styles.iconActive}>
        <Image
          alt={`On hover ${props.name} icon.`}
          src={adobephotoshop}/>
      </div>
      {
        props.tooltipID &&
        <ReactTooltip id={props.tooltipID} type="light" effect="solid">
          <span>{props.name}</span>
        </ReactTooltip>
      }
      <div className={styles.icon}>
        <Image
          alt={`Default ${props.name} icon.`}
          src={adobephotoshop}/>
      </div>
    </a>
  )
}*/
